---
title: "Event Markets API Errors"
sidebarTitle: "Errors"
description: "Handle Event Markets API HTTP errors and JSON error responses."
---

Errors use standard HTTP status codes and a JSON body with `label` and `message`.

```json
{
  "label": "invalid_request",
  "message": "Request parameters are invalid"
}
```

| Status | Meaning |
| --- | --- |
| `400` | Invalid request parameters |
| `401` | Gateway authentication is missing or invalid |
| `403` | The authenticated account is not allowed to access the resource |
| `404` | The requested resource does not exist |
| `429` | Rate limit exceeded |
| `5xx` | Server-side failure |

Use `label` for programmatic handling and `message` for diagnostics.
