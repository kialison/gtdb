---
title: "账户、手续费与领取"
sidebarTitle: "账户与领取"
description: "查询事件合约账户资产、资金流水、手续费以及结算后可领取金额。"
---

本页接口均为私有接口，需要认证。

## 账户资产

```text
GET /api/v4/event_markets/assets
```

常见字段包括 `fund_total`、`prediction_market_asset`、`prediction_position_value`、`spot_usdt_balance` 和 `unclaimed_amount`。

## 资金流水

```text
GET /api/v4/event_markets/account_book
```

支持 `limit` 与 `cursor`。

## 手续费

```text
GET /api/v4/event_markets/fee
```

可通过 `fee_type` 查询 `fixed` 或 `dynamic` 费率。

## 可领取金额

```text
GET /api/v4/event_markets/claims/available
GET /api/v4/event_markets/claims/available/{token_id}
```

## 发起领取

```text
POST /api/v4/event_markets/claims
```

## 查询领取结果

```text
GET /api/v4/event_markets/claims/{record_id}
```
