---
title: "WebSocket 概览"
sidebarTitle: "概览"
description: "连接事件合约 WebSocket，接收实时公开行情和认证账户事件。"
---

事件合约 WebSocket 用于实时市场数据和账户事件，通常与 REST API 配合使用：

- WebSocket：实时行情、订单、成交、持仓和结算推送；
- REST：历史查询、分页、订单管理以及断线后的状态恢复。

## 连接地址

```text
wss://broker.dexbuilder.com/v4/ws/event_contracts
```

当前协议版本为 **v2**，使用 JSON 消息。

## 公开频道

| 频道 | 用途 |
| --- | --- |
| `ec.trades` | 公开成交 |
| `ec.depth` | 完整订单簿深度 |
| `ec.bbo` | 最优买卖价与最新价 |
| `ec.cpi` | CPI 价格更新 |
| `ec.candlestick` | K 线更新 |
| `ec.oc_price` | 开盘 / 收盘参考价 |
| `ec.status` | 市场生命周期与可交易状态 |

## 私有频道

| 频道 | 用途 |
| --- | --- |
| `ec.orders` | 订单状态更新 |
| `ec.usertrades` | 当前用户成交 |
| `ec.positions` | 持仓更新 |
| `ec.settlements` | 结算更新 |

## 首次订阅示例

```json
{
  "t": 1787026512123,
  "id": "depth-1",
  "op": "subscribe",
  "ch": "ec.depth",
  "payload": {
    "items": [{"token_id":"gate_token_id","level":20}]
  }
}
```

## 重要约定

- `t` 为 Unix 毫秒时间戳；
- ID 应作为不透明字符串处理；
- 价格、数量、金额和费率使用字符串；
- 公开频道无需认证；
- 私有频道需要认证；
- 断线重连后需要重新认证并恢复订阅；
- WebSocket 不补发断线期间的数据，应使用 REST 进行恢复。
