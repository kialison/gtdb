---
title: Positions, Account, and User Trades
sidebarTitle: Positions, Account, and User Trades
description: Read private Event Contracts positions and account-related data.
---

## Authentication

All account endpoints are private and require an authenticated gateway request. Internal identity headers are not part of the public API contract.

## List positions

`GET /api/v4/event_contract/positions`

| Query parameter | Type | Description |
| --- | --- | --- |
| `event_id` | string | Event filter. |
| `condition_id` | string | Market condition filter. |
| `settlement_status` | string | Settlement-state filter. |
| `limit` | integer | Page size. |
| `cursor` | string | Previous `next_cursor`. |

Response is `items` plus `next_cursor`.

### Position fields

| Field | Type | Description |
| --- | --- | --- |
| `position_id` | string | Position identifier. |
| `event_id` / `token_id` | string | Event and outcome-token identifiers. |
| `outcome` | string | Outcome label. |
| `size` | string | Total position size. |
| `available_size` | string | Size available for a close or sell operation. |
| `locked_size` | string | Size locked by open orders or settlement. |
| `avg_open_price` | string | Average opening price. |
| `mark_price` | string | Current mark price. |
| `position_value` | string | Current marked value. |
| `claimable_amount` | string | Amount available to claim after settlement. |
| `settlement_status` | string | Settlement lifecycle state. |
| `won` | boolean | Whether the position's outcome won after resolution. |
| `updated_at` | integer | Last update timestamp. |

## Position detail and close

- `GET /api/v4/event_contract/positions/{position_id}`
- `POST /api/v4/event_contract/positions/{position_id}/close`

Close response:

```json
{"position_id":"position-123","status":"ACCEPTED"}
```

## User trades

`GET /api/v4/event_contract/usertrades`

Event Markets query parameters are `condition_id`, `from`, `to`, `sort_type`, `order_id`, `token_id`, `limit`, and `cursor`. Event Contracts supports the same market, token, order, time, and cursor filters.

Trade fields include `id`, `order_id`, `client_order_id`, `condition_id`, `token_id`, `price`, `size`, `side`, `role`, and `fee_rate`.

## Settled positions\\n\\n`GET /api/v4/event_contract/positions/settled` returns settled positions with the same `items` and `next_cursor` envelope. It accepts `limit` and `cursor`; each item uses the position fields described above.\\n\\n## Account endpoints

Event Contracts additionally exposes private account endpoints:

| Endpoint | Response purpose |
| --- | --- |
| `GET /api/v4/event_contract/assets` | Account asset summary. |
| `GET /api/v4/event_contract/balance` | Currency balance with `available`, `locked`, and `total`. |
| `GET /api/v4/event_contract/account_book` | Account ledger items. |
| `GET /api/v4/event_contract/fund_flows` | Fund-flow records. |
| `POST /api/v4/event_contract/transfer` | Transfer between supported accounts. |

## Error responses

| Status | Meaning |
| --- | --- |
| 400 | Invalid path, query, or request body. |
| 401 | Missing or invalid authentication. |
| 404 | Resource does not exist or is not visible to the account. |
| 503 | Upstream market-data dependency unavailable. |
