---
title: Get Order Book
sidebarTitle: Get Order Book
description: Get an Event Contracts token order book
---

## Access

Market-data endpoints are public. Private endpoints require gateway authentication. Internal gateway identity headers are not part of this public contract.

## Errors

`400` invalid request; `401` missing or invalid authentication; `404` unknown or invisible resource; `503` upstream market-data unavailable.

## GET `/api/v4/event_contract/tokens/{token_id}/order_book`

`depth` defaults to 20 and accepts 1–100. The response contains `token_id`, `condition_id`, `bids`, `asks`, `hash`, `timestamp`, `last_trade_price`, `min_order_size`, `tick_size`, and `neg_risk`; each price level has `price` and `size`.
